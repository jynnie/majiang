/**
 * GameEngine v1
 *
 * Mostly just built to play diff. version of mahjong
 */

import Paks from "../paks/Paks";

class GameEngine {
  id = "mhjng-dianxin";

  constructor(props) {
    console.log(props);
  }

  // startGame = (room, cardPakId) => {};
}

export default GameEngine;
